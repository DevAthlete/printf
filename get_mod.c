#include "main.h"

/**
 * get_mody - set all needed modifiers
 * @chr: modifier to set
 * @mody: pointer to modifiers
 *
 * Return: 1 if modifier set
 * 0 Otherwise
 */
int get_mody(char chr, mod_t *mody)
{
	(void) chr;
	(void) mody;
	return (0);
}

